# Per-page author overrides. Only needed if a page sets `author:` in its front matter;
# otherwise the sidebar uses the `author:` block in _config.yml.

Fuwen Li:
  name        : "Fuwen Li"
  email       : "Fuwen.Li@warwick.ac.uk"
  avatar      : "profile.png"
  github      : "li-f8"
